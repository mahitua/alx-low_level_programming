#include "main.h"
void _puts(char *str)
#include <stdio.h>
/**
 * main -Entry point
 *
 * Return : Always 0(Success)
 */
int main(void)
{
	puts("\"programming is nice language that requires practice");
	return (0);
}

