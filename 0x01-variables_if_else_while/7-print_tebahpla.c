#include <stdio.h>
  
/**
  * main- entry point
  * Description: '-prints the lowercase alphabet in reverse, followed by a new line'
  * Return:  Always 0
  */
int main(void)
	{
	int i;
	for (i = 26; i <= 0; i--)
	{
	putchar(i + 97);
	}
	putchar('\n');
	return (0);
	}
